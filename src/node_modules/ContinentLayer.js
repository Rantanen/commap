
/*global L */
"use strict";

var Game = require( 'GamePlanetside2' );

var ContinentLayer = L.LayerGroup.extend({
    initialize: function ( continent ) {
        var layer = L.tileLayer( '/maps/' + continent + '/{z}/{x}/{y}.jpg', {
            tms: true,
            noWrap: true
        });

        var layers = [ layer ];
        L.LayerGroup.prototype.initialize.call( this, layers );

        var HexLayer = require('planetside2/HexLayer');
        var hexLayer = new HexLayer( 'indar' );
        this.addLayer( hexLayer );
    }
});

module.exports = ContinentLayer;
